import React from 'react'

export default function App() {
  return (
    <div className="app">
      <header>
        <h1>Spring + React E‑commerce (Demo)</h1>
        <p>Starter frontend — add product listing and cart here.</p>
      </header>
      <main>
        <section className="hero">
          <p>This is a minimal scaffold. Run `npm install` then `npm run dev`.</p>
        </section>
      </main>
    </div>
  )
}
