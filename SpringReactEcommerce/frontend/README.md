# Frontend (React + Vite)

Quick start to create the frontend:

```bash
cd frontend
npm create vite@latest . -- --template react
npm install
npm run dev
```

I can scaffold a minimal Vite React app with a product listing and cart page next.
